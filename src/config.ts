export const CONFIG = {
    USERNAMES: [
        "Alice",
        "Bob",
        "Charlie",
        "David",
    ],
    DEBUG_AUTO_START: import.meta.env.DEV ? false : false,
    DEBUG_INITIAL_STATE: import.meta.env.DEV ? false : false,
    DEBUG_LOG_DIFFS: import.meta.env.DEV ? true : true,
}
